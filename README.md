# Central de Enablement · Time de CS

Hub interativo para o time aprender a usar o Claude na rotina: configurar o
ambiente (VS Code + Claude Code + GitHub), engenharia de prompt, MCP e casos
reais das reuniões.

Site estático de uma página só (`index.html`), publicado via GitHub Pages.
Para atualizar: edite o `index.html`, faça commit e push — o site atualiza sozinho.
